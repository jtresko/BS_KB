title: BigShort Help Center
description: Guides, tutorials, and documentation to help you get the most out of BigShort
theme: just-the-docs

url: https://jtresko.github.io/BS_KB

repository: https://github.com/jtresko/BS_KB # for github-metadata

# Logo - displays in top left of navigation
logo: "/assets/images/bs_logo.png"

# Favicon
favicon_ico: "/assets/images/bs_logo.png"

nav_enabled: true
heading_anchors: true

color_scheme: dark

# Hide "Just the Docs" branding
gh_edit_link: false

# Enable or disable the site search
# Supports true (default) or false
search_enabled: true
search:
  # Split pages into sections that can be searched individually
  # Supports 1 - 6, default: 2
  heading_level: 2
  # Maximum amount of previews per search result
  # Default: 3
  previews: 2
  # Maximum amount of words to display before a matched word in the preview
  # Default: 5
  preview_words_before: 3
  # Maximum amount of words to display after a matched word in the preview
  # Default: 10
  preview_words_after: 3
  # Set the search token separator
  # Default: /[\s\-/]+/
  # Example: enable support for hyphenated search words
  tokenizer_separator: /[\s/]+/
  # Display the relative url in search results
  # Supports true (default) or false
  rel_url: true
  # Enable or disable the search button that appears in the bottom right corner of every page
  # Supports true or false (default)
  button: false
  # Focus the search input by pressing `ctrl + focus_shortcut_key` (or `cmd + focus_shortcut_key` on macOS)
  focus_shortcut_key: "k"

# For copy button on code
enable_copy_code_button: true

# By default, consuming the theme as a gem leaves mermaid disabled; it is opt-in
mermaid:
  # Version of mermaid library
  # Pick an available version from https://cdn.jsdelivr.net/npm/mermaid/
  version: "9.1.6"
  # Put any additional configuration, such as setting the theme, in _includes/mermaid_config.js
  # See also docs/ui-components/code
  # To load mermaid from a local library, also use the `path` key to specify the location of the library; e.g.
  # for (v10+):
  # path: "/assets/js/mermaid.esm.min.mjs"
  # for (<v10):
  # path: "/assets/js/mermaid.min.js"
  # Note: copy both `mermaid.esm.min.mjs` (v10+) or `mermaid.min.js` (<v10) and the associated `.map` file from the specified version of `mermaid/dist` to `/assets/js/`.


# Exclude folders from Jekyll processing
exclude:
  - intercom_export/
  - scripts/
  - Gemfile
  - Gemfile.lock
  - LICENSE
  - README.md
  - jekyll_output.log
  - "*.log"
  - "Onboarding Guide/"

plugins:
  - jekyll-seo-tag
  - jekyll-include-cache


# Back to top link
back_to_top: true
back_to_top_text: "Back to top"

footer_content: '© 2025 BigShort. All rights reserved.'

# Auxiliary links in the header
aux_links:
  BigShort App: https://bigshort.com

# Makes aux links open in new tab
aux_links_new_tab: true

# Custom callouts
callouts_level: quiet
callouts:
  tip:
    title: Tip
    color: green
  note:
    title: Note
    color: blue
  warning:
    title: Warning
    color: yellow
